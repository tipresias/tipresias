import groupModelsByRound from '../index';

describe('groupModelsByRound', () => {
  it('works', () => {
    // arrange
    // act
    // const result = createBarGroups();
    // assert
    expect(true).toBe(true);
  });
});
