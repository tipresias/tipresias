name: build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "21"
      - name: Install dependencies
        run: yarn
      - name: Run unit tests
        run: npx jest
