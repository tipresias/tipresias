import createBarGroups from '../index';

describe('createBarGroups', () => {
  it('works', () => {
    // arrange
    // act
    // const result = createBarGroups();
    // assert
    expect(true).toBe(true);
  });
});
