import createCumulativeModels from '../index';

describe('createCumulativeModels', () => {
  it('works', () => {
    // arrange
    // act
    // const result = createBarGroups();
    // assert
    expect(true).toBe(true);
  });
});
