import createScale from '../index';

describe('createScale', () => {
  it('works', () => {
    // arrange
    // act
    // const result = createBarGroups();
    // assert
    expect(true).toBe(true);
  });
});
